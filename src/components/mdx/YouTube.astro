---
import { YouTube as YT } from "@astro-community/astro-embed-youtube";

interface Props {
  id: string;
}

const { id } = Astro.props;
---

<div class="relative w-full h-0 pb-[56.25%] my-6">
  <YT id={id} class="absolute top-0 left-0 w-full h-full" />
</div>
