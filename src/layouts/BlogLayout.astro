---
import "@styles/global.css";
import Header from "@components/Header";
import Footer from "@components/Footer.astro";
import BaseHead from "../components/BaseHead.astro";
import { metaData } from "../config";

export interface Props {
  title: string;
  description: string;
  author?: string;
  date: string | Date;
  readTime: string;
  category: string;
  featured?: boolean;
  slug: string;
  cover?: string;
  excerpt?: string;
}

const {
  title,
  description,
  author = metaData.name,
  date,
  readTime,
  category,
  featured = false,
  cover,
} = Astro.props;

const dateObj = new Date(date);
const formattedDate = new Intl.DateTimeFormat("en-US", {
  dateStyle: "long",
}).format(dateObj);
const isoDate = dateObj.toISOString().slice(0, 10);
---

<html lang="en">
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta
      name="googlebot"
      content="index, follow, max-video-preview:-1, max-image-preview:large, max-snippet:-1"
    />
    <link rel="sitemap" href="/sitemap-index.xml" />
    <BaseHead title={title} description={description} image={cover} />
    <link
      rel="alternate"
      type="application/rss+xml"
      title={metaData.title}
      href={new URL("rss.xml", Astro.site)}
    />
  </head>
  <body class="antialiased">
    <div class="min-h-screen">
      <Header client:load />
      <main class="pt-16">
        <article class="py-18">
          <div class="mx-auto max-w-[800px] px-6 sm:px-12">
            <!-- Back to Blog -->
            <div class="mb-8">
              <a
                href="/blog"
                class="inline-flex items-center gap-2 no-underline text-xs font-medium tracking-widest uppercase text-ind-accent border border-ind-accent px-4 py-2 transition-colors duration-[240ms] hover:bg-ind-accent hover:text-text-on-accent"
              >
                ← Back to Blog
              </a>
            </div>

            <!-- Article Header -->
            <header class="mb-12 pb-8 border-b border-border-soft">
              <div class="flex items-center gap-3 mb-5">
                <span class="text-xs font-semibold tracking-widest uppercase text-text-on-accent bg-ind-accent px-3 py-1.5">
                  {category}
                </span>
                {featured && (
                  <span class="text-xs font-bold tracking-widest uppercase text-ind-accent bg-bg-main border border-ind-accent px-2.5 py-1">
                    Featured
                  </span>
                )}
              </div>

              <h1 class="text-3xl sm:text-4xl font-bold tracking-tight text-text-primary leading-tight mb-5">
                {title}
              </h1>

              <div class="flex flex-wrap items-center gap-6 mb-5">
                <span class="text-sm text-text-secondary">
                  {author}
                </span>
                <time datetime={isoDate} class="text-sm text-text-secondary">
                  {formattedDate}
                </time>
                <span class="text-sm text-text-secondary">
                  {readTime}
                </span>
              </div>

              <p class="text-lg leading-relaxed text-text-secondary max-w-[640px]">
                {description}
              </p>
            </header>

            <!-- Article Content -->
            <div class="prose prose-lg max-w-none blog-prose">
              <slot />
            </div>

            <!-- CTA -->
            <div class="bg-surface p-7 mt-12 border border-border-soft">
              <div class="text-base font-bold text-text-primary mb-2">
                What's Next?
              </div>
              <p class="text-sm leading-relaxed text-text-secondary mb-4">
                Want to dive deeper into web development? Check out my other
                posts on building scalable applications and modern development
                practices.
              </p>
              <a
                href="/blog"
                class="inline-block no-underline text-xs font-semibold tracking-widest uppercase bg-ind-accent text-text-on-accent px-6 py-3 transition-opacity duration-[240ms] hover:opacity-90"
              >
                View All Posts →
              </a>
            </div>
          </div>
        </article>
      </main>
      <Footer />
    </div>
  </body>
</html>

<style>
  /* Prose styles target slot-rendered MDX content via :global(); Tailwind utilities cannot reach these. */
  .blog-prose {
    color: var(--text-secondary);
    font-size: 1rem;
    line-height: 1.75;
  }
  .blog-prose :global(h1),
  .blog-prose :global(h2),
  .blog-prose :global(h3),
  .blog-prose :global(h4) {
    color: var(--text-primary);
    font-weight: 700;
    letter-spacing: -0.025em;
    text-transform: uppercase;
  }
  .blog-prose :global(h2) {
    font-size: 1.5rem;
    margin-top: 3rem;
    margin-bottom: 1rem;
    padding-bottom: 0.75rem;
    border-bottom: 1px solid var(--border-soft);
  }
  .blog-prose :global(h3) {
    font-size: 1.125rem;
    margin-top: 2rem;
    margin-bottom: 0.75rem;
  }
  .blog-prose :global(p) {
    margin-bottom: 1.25rem;
  }
  .blog-prose :global(a) {
    color: var(--ind-accent);
    text-decoration: underline;
    text-underline-offset: 3px;
  }
  .blog-prose :global(a:hover) {
    color: var(--ind-accent-hover);
  }
  .blog-prose :global(pre) {
    background: var(--surface) !important;
    border: 1px solid var(--border-medium);
    padding: 1.25rem;
    margin: 1.5rem 0;
    overflow-x: auto;
    font-size: 0.875rem;
  }
  .blog-prose :global(blockquote) {
    border-left: 3px solid var(--ind-accent);
    padding-left: 1.25rem;
    color: var(--text-secondary);
    font-style: italic;
    margin: 1.5rem 0;
  }
  .blog-prose :global(ul),
  .blog-prose :global(ol) {
    padding-left: 1.25rem;
    margin-bottom: 1.25rem;
  }
  .blog-prose :global(li) {
    color: var(--text-secondary);
    font-size: 1rem;
    line-height: 1.75;
    margin-bottom: 0.5rem;
  }
  .blog-prose :global(img) {
    max-width: 100%;
    margin: 1.5rem 0;
  }
  .blog-prose :global(hr) {
    border: none;
    border-top: 1px solid var(--border-medium);
    margin: 2rem 0;
  }
  .blog-prose :global(table) {
    width: 100%;
    border: 1px solid var(--border-strong);
    border-collapse: collapse;
    margin: 1.5rem 0;
  }
  .blog-prose :global(th),
  .blog-prose :global(td) {
    border: 1px solid var(--border-strong);
    padding: 0.625rem 0.875rem;
    font-size: 0.875rem;
  }
  .blog-prose :global(th) {
    background: var(--surface);
    color: var(--text-primary);
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    font-size: 0.75rem;
  }
</style>
